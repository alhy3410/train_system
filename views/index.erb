<h1>Welcome to the Train System Database!</h1>

<p><a href ='/cities'> Add a new City </a></p>
<p><a href ='/trains'> Add a new Train</a></p>

<h3>Here is a list of all trains in this Database</h3>

<% if @trains.any?() %>
  <p>Click on a train to see information about them and mark which city they go to:</p>

  <ul>
    <% @trains.each do |train| %>
      <li><a href='/trains/<%= train.id() %>'><%= train.name() %></a></li>
    <% end %>
  </ul>

  <% else %>
      <p>There are no Trains added yet!</p>
  <% end %>

  <h3>Here is a list of all Cities in this Database</h3>

  <% if @cities.any?() %>
  <p>Click on a City to see information about them and mark which train goes there:</p>

  <ul>
    <% @cities.each do |city| %>
    <li><a href='/cities/<%= city.id() %>'><%= city.name() %></a></li>
    <% end %>
  </ul>

  <% else %>
  <p>There are no Cities added yet!</p>
  <% end %>
